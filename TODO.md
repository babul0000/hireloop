# TODO
- [ ] Fix `src/app/dashboard/recruiter/page.jsx` loading condition (missing `return` when `isPending`).
- [ ] (Next) Validate `src/app/dashboard/recruiter/jobs/page.jsx` fetch URL / env guard and remove hardcoded `companyId` if possible after auth/session wiring.
